name: RBAC Schema Definition

namespaces:
  - batch_changes:
    name: "BATCHCHANGES"
    actions:
      - VIEW
      - CREATE
      - EXECUTE

  - notebooks:
    name: "NOTEBOOKS"
    actions:
      - VIEW
      - CREATE

  - codeinsights:
    name: "CODEINSIGHTS"
    actions:
      - VIEW
      - CREATE

  # - orgs:
  #   name: "ORG"
  #   actions:
  #     - MEMBER
  #     - ADMIN